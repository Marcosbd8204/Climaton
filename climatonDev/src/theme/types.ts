export type ThemeType = {
  mode: 'light' | 'dark';
  colors: {
    background: string;
    primary: string;
    secondary: string;
    text: string;
    textSecondary: string;
    white: string;
    button: string;
  };
};